{
  "name": "copilot-superclaude-tools",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Read-only Jira/Confluence/GitLab script tools. No dependencies — Node 20+'s built-in fetch is enough. This file exists only to declare ESM (\"type\": \"module\") for jira/, confluence/, gitlab/, and lib/ — Node resolves it by walking up from each script, so one file here covers all four.",
  "type": "module"
}
